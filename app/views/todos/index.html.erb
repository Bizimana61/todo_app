<% content_for :title, "Todos" %>

<div class="hero" role="region" aria-labelledby="hero-heading">
  <div class="container">
    <h2 id="hero-heading">Welcome to TaskFlow</h2>
    <p>Do you want some help organizing your todo list today? Click the button below to get started with a few helpful tips.</p>
    <p>
      <a class="btn" href="#organize-help">Yes — help me organize</a>
      <%= link_to 'Create new todo', new_todo_path, class: 'btn secondary' %>
    </p>
  </div>
</div>

<section class="container" aria-labelledby="todos-heading">
  <h1 id="todos-heading">Your Todos</h1>

  <div id="todos" class="todo-list" role="list">
    <%= render @todos %>
  </div>

  <p><%= link_to "New todo", new_todo_path, class: 'btn' %></p>
</section>
<%= render 'shared/organize_help' %>